name: my_clean_architecture
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.40 <0.1.0"

vars:
  feature:
    type: string
    description: Your feature
    default: main
    prompt: What is your feature's name?
